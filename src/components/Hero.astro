---
/* Aquí no necesitas lógica de JS todavía */
---

<section class="hero">
  <!-- Video de fondo -->
  <video class="hero-video" autoplay muted loop playsinline>
    <source src="../vids/video_hero.mp4" type="video/mp4" />
    Tu navegador no soporta el video.
  </video>
  <div class="hero-overlay"></div>
  <div class="hero-content">
    <h1 class="hero-title">
      Conecta, Aprende y <span class="highlight">Domina</span><br />
      el idioma inglés
    </h1>

    <p class="hero-description">
      Mejora tu inglés con nuestras clases virtuales,
      diseñadas para que aprendas de forma práctica y divertida.
      Con profesionales con experiencia y una comunidad de estudiantes como tú.
    </p>

    <div class="hero-actions">
      <a href="/contacto" class="btn btn-primary">
        <span>Empieza hoy</span>
        <div class="btn-shine"></div>
      </a>
      <a href="/cursos" class="btn btn-secondary">
        <span>Contacto</span>
        <svg width="16" height="16" viewBox="0 0 16 16" fill="none">
          <path
            d="M6 12L10 8L6 4"
            stroke="currentColor"
            stroke-width="2"
            stroke-linecap="round"
            stroke-linejoin="round"
          />
        </svg>
      </a>
    </div>
  </div>
</section>

<style>
  .hero {
    position: relative;
    display: flex;
    align-items: center;
    justify-content: center;
    min-height: 100vh;
    padding: 2rem;
    overflow: hidden;
  }

  .hero-video {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    object-fit: cover;
    z-index: 0;
  }

  .hero-content {
    position: relative;
    z-index: 1;
    text-align: center;
    max-width: 900px;
    width: 100%;
    color: #F5F5F3;
    margin-top: -120px;
  }

  .hero-title {
    font-size: clamp(2.5rem, 5vw, 4rem);
    font-weight: 800;
    line-height: 1.1;
    margin-bottom: 1.5rem;
  }

  .highlight {
    background: linear-gradient(135deg, #FC725B, #f59e0b);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
  }

  .hero-description {
    font-size: 1.125rem;
    line-height: 1.7;
    color: #F5F5F3;
    max-width: 700px;
    margin: 0 auto 3rem;
  }

  .hero-actions {
    display: flex;
    gap: 1rem;
    justify-content: center;
    flex-wrap: wrap;
    margin-bottom: 2rem;
  }

  .btn {
    position: relative;
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    padding: 1rem 2rem;
    border-radius: 12px;
    font-weight: 600;
    font-size: 1rem;
    text-decoration: none;
    transition: all 0.3s ease;
    cursor: pointer;
    overflow: hidden;
  }

  .btn-primary {
    background: linear-gradient(135deg, #174A63, #2563eb);
    color: white;
  }

  .btn-secondary {
    background: rgba(255, 255, 255, 0.9);
    color: #174A63;
    border: 2px solid rgba(23, 74, 99, 0.2);
    backdrop-filter: blur(10px);
  }
  /* Botón primario hover */
.btn-primary:hover {
  transform: translateY(-2px);
  box-shadow: 0 8px 30px rgba(23, 74, 99, 0.4);
}

.btn-shine {
  position: absolute;
  top: 0;
  left: -100%;
  width: 100%;
  height: 100%;
  background: linear-gradient(90deg, transparent, rgba(255,255,255,0.3), transparent);
  transition: left 0.6s;
}

.btn-primary:hover .btn-shine {
  left: 100%;
}

/* Botón secundario hover */
.btn-secondary:hover {
  background: #174A63;
  color: white;
  border-color: #174A63;
  transform: translateY(-2px);
  box-shadow: 0 8px 30px rgba(23, 74, 99, 0.3);
}

.btn-secondary svg {
  transition: transform 0.3s ease;
}

.btn-secondary:hover svg {
  transform: translateX(2px);
}

  .hero-overlay {
  position: absolute;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  background: rgba(0, 0, 0, 0.7); /* negro semi-transparente */
  z-index: 0; /* detrás del contenido */
}

</style>
